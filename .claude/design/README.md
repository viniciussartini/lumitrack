# Handoff bundles do Claude Design

Um diretório por tela/fluxo: `{YYYY-MM-DD}-<tela-ou-fluxo>/` (o mais recente por data é o vigente; anteriores são histórico).

Fluxo: Claude Design → **Export → Handoff to Claude Code → Send to local coding agent** → salvar aqui. Regras completas em `../project_context/10-design-system.md`.
